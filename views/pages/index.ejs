<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Auth System Demo</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="container text-center">
        <h1>ยินดีต้อนรับ</h1>
        <p class="text-muted">ระบบยืนยันตัวตนด้วย Node.js, Express และ SQLite</p>

        <% if (user) { %>
            <div class="card" style="border:none; box-shadow:none; background: #f9fafb;">
                <p>สวัสดี, <strong>
                        <%= user.username %>
                    </strong></p>
                <a href="/profile" class="btn">ไปที่หน้าโปรไฟล์</a>
                <div class="mt-4">
                    <a href="/auth/logout" class="text-link" style="color: var(--danger-color);">ออกจากระบบ</a>
                </div>
            </div>
            <% } else { %>
                <div style="display: flex; flex-direction: column; gap: 10px;">
                    <a href="/auth/login" class="btn">เข้าสู่ระบบ</a>
                </div>
                <% } %>

                    <div class="info">
                        <p><strong>การใช้งาน API:</strong><br>
                            POST <code>/api/login</code> เพื่อรับ Token</p>
                    </div>
    </div>
</body>

</html>